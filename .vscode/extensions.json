{
  // Без этого расширения .astro для редактора — обычный текст: подсветки нет,
  // переходов по компонентам нет, ошибки шаблона видны только в сборке.
  "recommendations": ["astro-build.astro-vscode"]
}
